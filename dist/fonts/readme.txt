The fonts folder is here because bootstrap 3 expects the fonts for the glyphicons to be present in a folder parallel to the css folder

This folder is copied from the path
/node_modules/bootstrap/dist/fonts